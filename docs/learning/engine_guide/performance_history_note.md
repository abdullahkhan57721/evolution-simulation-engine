# How to Read Historical Performance Notes

Performance case studies in this textbook teach decision patterns, not permanent
benchmark values.

Treat historical measurements as:

```text
evidence that justified a past decision
```

not:

```text
a promise that the same function always takes the same milliseconds
```

For current performance facts, use `docs/performance.md`, current profiler scripts,
and fresh comparable measurements.
