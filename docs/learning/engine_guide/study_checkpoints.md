# Study Checkpoints

Use these as short retrieval checkpoints between larger sections.

## Checkpoint A — architecture

Without notes, explain:

```text
abstraction vs implementation
dependency direction
state vs context
ownership vs authority
```

## Checkpoint B — simulation

Draw:

```text
committed state -> working copy -> stages -> commit
```

Then state what happens to RNG on failure.

## Checkpoint C — evolution

Explain evolution without using:

```text
gene
genome
organism
reproduction
```

Then map the abstract terms back to biology.

## Checkpoint D — kernel

Write the four stage phases and explain why each boundary exists.

## Checkpoint E — engineering

For `StageCoordinator`, define `P`, `Q`, and `R`, write its structural complexity,
and name one memory lifetime and one optimization boundary.

## Checkpoint F — design

Take a new biological feature and answer:

```text
owning layer?
mutable state?
configuration/policy?
proposal?
conflict?
randomness timing?
materialization?
mutation owner?
telemetry?
scaling variables?
```
